<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ui.OverallWeatherFragment">

    <include
        android:id="@+id/current_weather_layout"
        layout="@layout/current_weather_layout"/>

    <include
        android:id="@+id/hourly_weather_recycler"
        layout="@layout/hourly_weather_recycler"/>

    <include
        android:id="@+id/daily_weather_recycler"
        layout="@layout/daily_weather_recycler"/>

</LinearLayout>
</layout>